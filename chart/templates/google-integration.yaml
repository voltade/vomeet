{{- if index .Values "google-integration" "enabled" }}
{{- $svc := "google-integration" }}
{{- $config := index .Values $svc }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vomeet.fullname" . }}-{{ $svc }}
  labels:
    {{- include "vomeet.serviceLabels" (dict "root" . "component" $svc) | nindent 4 }}
spec:
  replicas: {{ $config.replicaCount }}
  selector:
    matchLabels:
      {{- include "vomeet.serviceSelectorLabels" (dict "root" . "component" $svc) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "vomeet.serviceSelectorLabels" (dict "root" . "component" $svc) | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: {{ $svc }}
          image: {{ include "vomeet.image" (dict "root" . "service" $svc) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: {{ $config.port }}
              protocol: TCP
          env:
            - name: DB_HOST
              value: {{ include "vomeet.dbHost" . }}
            - name: DB_PORT
              value: {{ include "vomeet.dbPort" . | quote }}
            - name: DB_NAME
              value: {{ .Values.global.database.name }}
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "vomeet.fullname" . }}-db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "vomeet.fullname" . }}-db-credentials
                  key: password
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml $config.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "vomeet.fullname" . }}-{{ $svc }}
  labels:
    {{- include "vomeet.serviceLabels" (dict "root" . "component" $svc) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ $config.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vomeet.serviceSelectorLabels" (dict "root" . "component" $svc) | nindent 4 }}
{{- end }}
