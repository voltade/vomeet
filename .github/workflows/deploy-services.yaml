name: Deploy Services

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy (default: latest)"
        required: false
        default: "latest"
  workflow_run:
    workflows: ["Build Services"]
    types:
      - completed
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/vomeet
  NAMESPACE: vomeet

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    # Only run if workflow_dispatch OR if build succeeded
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set image tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "tag=${{ inputs.image_tag }}" >> $GITHUB_OUTPUT
          else
            # For workflow_run, use latest
            echo "tag=latest" >> $GITHUB_OUTPUT
          fi

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Check if images exist
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          SERVICES="admin-api api-gateway bot-manager transcription-collector"
          MISSING=""
          for svc in $SERVICES; do
            IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${svc}:${TAG}"
            if ! docker manifest inspect "$IMAGE" > /dev/null 2>&1; then
              MISSING="${MISSING}${svc} "
            fi
          done
          if [ -n "$MISSING" ]; then
            echo "❌ Missing images for tag ${TAG}: ${MISSING}"
            exit 1
          fi
          echo "✅ All images exist for tag ${TAG}"

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: 'v1.32.2'

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: '3.16.4'

      - name: Create kubeconfig
        run: |
          mkdir -p ~/.kube
          cat > ~/.kube/config << EOF
          apiVersion: v1
          kind: Config
          clusters:
            - name: ${{ vars.KUBE_CLUSTER_NAME_PROD }}
              cluster:
                server: ${{ vars.KUBE_CLUSTER_URL_PROD }}
                certificate-authority-data: ${{ secrets.KUBE_CERTIFICATE_PROD }}
          contexts:
            - name: ${{ vars.KUBE_CLUSTER_NAME_PROD }}
              context:
                cluster: ${{ vars.KUBE_CLUSTER_NAME_PROD }}
                namespace: ${{ env.NAMESPACE }}
                user: vomeet-admin
          users:
            - name: vomeet-admin
              user:
                token: ${{ secrets.KUBE_TOKEN_PROD }}
          current-context: ${{ vars.KUBE_CLUSTER_NAME_PROD }}
          EOF
          chmod 600 ~/.kube/config

      - name: Update Helm dependencies
        run: |
          helm dependency update ./chart

      - name: Create namespace
        run: |
          kubectl create namespace ${{ env.NAMESPACE }} --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy Helm chart
        env:
          TAG: ${{ steps.tag.outputs.tag }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          ADMIN_API_TOKEN: ${{ secrets.ADMIN_API_TOKEN }}
        run: |
          helm upgrade --install vomeet ./chart \
            --namespace ${{ env.NAMESPACE }} \
            --set global.imageTag="$TAG" \
            --set postgresql.password="$DB_PASSWORD" \
            --set adminApiToken="$ADMIN_API_TOKEN" \
            --wait \
            --timeout 10m

      - name: Verify deployment
        run: |
          echo "Checking deployments..."
          kubectl get deployments -n ${{ env.NAMESPACE }}
          echo ""
          echo "Checking pods..."
          kubectl get pods -n ${{ env.NAMESPACE }}
          echo "✅ Deployment verified"

      - name: Cleanup kubeconfig
        if: always()
        run: rm -f ~/.kube/config

      - name: Send success notification to Telegram
        if: success()
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d message_thread_id="${{ secrets.TELEGRAM_THREAD_ID }}" \
            -d text="✅ *Vomeet Deployed*%0A%0A📦 Services: all%0A🏷️ Image Tag: ${{ env.TAG }}%0A👤 Triggered by: ${{ github.actor }}%0A⏰ Time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')%0A🔗 [View Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" \
            -d parse_mode="Markdown"

      - name: Send failure notification to Telegram
        if: failure()
        env:
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d message_thread_id="${{ secrets.TELEGRAM_THREAD_ID }}" \
            -d text="❌ *Vomeet Deploy Failed*%0A%0A🏷️ Image Tag: ${{ env.TAG }}%0A👤 Triggered by: ${{ github.actor }}%0A⏰ Time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')%0A🔗 [View Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" \
            -d parse_mode="Markdown"
