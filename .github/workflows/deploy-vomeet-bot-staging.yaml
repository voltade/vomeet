name: Deploy vomeet-bot Staging

on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/vomeet-bot
  NAMESPACE: vomeet-bot-staging

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Check if staging image exists
        run: |
          docker manifest inspect ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging > /dev/null 2>&1
          if [ $? -eq 0 ]; then
            echo "✅ Staging image exists in registry"
          else
            echo "❌ Staging image not found in registry!"
            exit 1
          fi

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: 'v1.32.2'

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: '3.16.4'

      - name: Create kubeconfig
        run: |
          mkdir -p ~/.kube
          cat > ~/.kube/config << EOF
          apiVersion: v1
          kind: Config
          clusters:
            - name: ${{ vars.KUBE_CLUSTER_NAME_STAGING }}
              cluster:
                server: ${{ vars.KUBE_CLUSTER_URL_STAGING }}
                certificate-authority-data: ${{ secrets.KUBE_CERTIFICATE_STAGING }}
          contexts:
            - name: ${{ vars.KUBE_CLUSTER_NAME_STAGING }}
              context:
                cluster: ${{ vars.KUBE_CLUSTER_NAME_STAGING }}
                namespace: ${{ env.NAMESPACE }}
                user: vomeet-bot-admin
          users:
            - name: vomeet-bot-admin
              user:
                token: ${{ secrets.KUBE_TOKEN_STAGING }}
          current-context: ${{ vars.KUBE_CLUSTER_NAME_STAGING }}
          EOF

      - name: Create namespace if not exists
        run: |
          kubectl create namespace ${{ env.NAMESPACE }} --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy Helm chart
        run: |
          helm upgrade --install vomeet-bot ./services/vomeet-bot/chart \
            --namespace ${{ env.NAMESPACE }} \
            --values ./services/vomeet-bot/chart/values.yaml \
            --values ./services/vomeet-bot/chart/values.staging.yaml \
            --set imageTag=staging \
            --set imageRepository=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }} \
            --wait \
            --timeout 5m

      - name: Verify deployment
        run: |
          echo "Checking deployed resources..."
          kubectl get serviceaccount -n ${{ env.NAMESPACE }}
          kubectl get roles -n ${{ env.NAMESPACE }}
          kubectl get rolebindings -n ${{ env.NAMESPACE }}
          kubectl get externalsecrets -n ${{ env.NAMESPACE }} || true
          echo "✅ Deployment verified"

      - name: Cleanup kubeconfig
        if: always()
        run: rm -f ~/.kube/config

      - name: Send success notification to Telegram
        if: success()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d message_thread_id="${{ secrets.TELEGRAM_THREAD_ID }}" \
            -d text="✅ *[STAGING] vomeet-bot Deployed*%0A%0A📦 Namespace: ${{ env.NAMESPACE }}%0A🔀 Branch: ${{ github.ref_name }}%0A👤 Triggered by: ${{ github.actor }}%0A⏰ Time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')%0A🔗 [View Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" \
            -d parse_mode="Markdown"

      - name: Send failure notification to Telegram
        if: failure()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d message_thread_id="${{ secrets.TELEGRAM_THREAD_ID }}" \
            -d text="❌ *[STAGING] vomeet-bot Deploy Failed*%0A%0A📦 Namespace: ${{ env.NAMESPACE }}%0A🔀 Branch: ${{ github.ref_name }}%0A👤 Triggered by: ${{ github.actor }}%0A⏰ Time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')%0A🔗 [View Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" \
            -d parse_mode="Markdown"
